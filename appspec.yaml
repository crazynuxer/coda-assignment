version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: "arn"
        LoadBalancerInfo:
          ContainerName: "nginx"
          ContainerPort: 80
